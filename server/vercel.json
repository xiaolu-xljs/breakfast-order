{
  "version": 2,
  "buildCommand": "prisma generate && prisma db push --skip-generate --accept-data-loss && node prisma/seed.js",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api"
    }
  ]
}